'use client';
import * as React from 'react';





  export interface ItemsOverviewProps {
/** Shopping cart object from which the cart items overview will be displayed */
cart: Cart;

/** The CSS class for the cart items overview container */
itemsOverviewContainerClass?: string;

/** Title of the cart items overview */
title?: string;

/** The cart items names are clickable links */
itemNameClickable?: boolean;

/** Action when a cart item's name is clicked */ onCartItemNameClick?: (item: CartMainItem) => void;  /** Show the quantity of the cart item */ showQuantity?: boolean;  /** Show the availability of the cart item */ showAvailability?: boolean;  /** Show the SKU of the cart item */ showSku?: boolean;  /** Show a small image of the cart item */ showImage?: boolean;  /** Show the price of the cart item */ showPrice?: boolean;  /** Custom price formatting function */ formatPrice?: (price: number) => string;  /** Labels for the component */ labels?: Record<string, string>; }    import  { Cart, CartMainItem } from 'propeller-sdk-v2';      function ItemsOverview(props:ItemsOverviewProps) {    function containerClass() { return props.itemsOverviewContainerClass || 'cart-items-overview'; }   function itemNameClickable() { return props.itemNameClickable !== undefined ? props.itemNameClickable : true; }   function showQuantity() { return props.showQuantity !== undefined ? props.showQuantity : true; }   function showAvailability() { return props.showAvailability !== undefined ? props.showAvailability : true; }   function showSku() { return props.showSku !== undefined ? props.showSku : true; }   function showImage() { return props.showImage !== undefined ? props.showImage : true; }   function showPrice() { return props.showPrice !== undefined ? props.showPrice : true; }   function getLabel(key: string, fallback: string) { return props.labels?.[key] || fallback; }   function formatItemPrice(price: number) { if (props.formatPrice) { return props.formatPrice(price); } return '\u20AC' + Number(price || 0).toFixed(2); }   function items() { return (props.cart as any)?.items || []; }   function getItemName(item: any) { return item.product?.names?.[0]?.value || 'Product'; }   function getItemSku(item: any) { return item.product?.sku || ''; }   function getItemImageUrl(item: any) { const url = item.product?.media?.images?.items?.[0]?.imageVariants?.[0]?.url; if (url && typeof url === 'string' && url.startsWith('http')) { return url; } return ''; }   function getItemTotalPrice(item: any) { return (item.price || 0) * (item.quantity || 1); }   function getItemAvailability(item: any) { const stock = item.product?.inventory?.totalQuantity; if (stock === undefined || stock === null) return ''; if (stock > 0) return getLabel('inStock', 'In stock'); return getLabel('outOfStock', 'Out of stock'); }   function isInStock(item: any) { const stock = item.product?.inventory?.totalQuantity; return stock !== undefined && stock !== null && stock > 0; }   function handleItemNameClick(item: any) { if (itemNameClickable() && props.onCartItemNameClick) { props.onCartItemNameClick(item as CartMainItem); } }            return (     <div  className={containerClass()}>{props.title ? (   <h2 className="text-lg font-bold mb-4">{props.title}</h2> ) : null}<div className="space-y-4">{items()?.map((item, index) => (   <div className="flex items-center gap-3 pb-3 border-b border-gray-200 last:border-b-0 last:pb-0"  key={item.itemId || index}>{showImage() ? (   <div className="w-12 h-12 bg-gray-100 rounded-md flex items-center justify-center overflow-hidden flex-shrink-0">{getItemImageUrl(item) ? (   <img className="w-full h-full object-contain p-1"  src={getItemImageUrl(item)}  alt={getItemName(item)}  /> ) : null}{!getItemImageUrl(item) ? (   <svg  fill="none"  viewBox="0 0 24 24"  stroke="currentColor"  strokeWidth="1.5" className="w-6 h-6 text-gray-300"><path  strokeLinecap="round"  strokeLinejoin="round"  d="M2.25 15.75l5.159-5.159a2.25 2.25 0 013.182 0l5.159 5.159m-1.5-1.5l1.409-1.409a2.25 2.25 0 013.182 0l2.909 2.909M3.75 21h16.5A2.25 2.25 0 0022.5 18.75V5.25A2.25 2.25 0 0020.25 3H3.75A2.25 2.25 0 001.5 5.25v13.5A2.25 2.25 0 003.75 21z"  /></svg> ) : null}</div> ) : null}<div className="flex-1 min-w-0">{itemNameClickable() ? (   <p className="font-medium text-sm truncate cursor-pointer hover:text-violet-600 transition-colors"  onClick={(event) => handleItemNameClick(item) }>{getItemName(item)}</p> ) : null}{!itemNameClickable() ? (   <p className="font-medium text-sm truncate">{getItemName(item)}</p> ) : null}{showSku() && getItemSku(item) ? (   <p className="text-xs text-gray-500">{getItemSku(item)}</p> ) : null}{showQuantity() ? (   <p className="text-xs text-gray-500">{getLabel('quantity', 'Qty:')}{item.quantity}</p> ) : null}{showAvailability() && getItemAvailability(item) ? (   <p  className={`text-xs ${isInStock(item) ? 'text-green-600' : 'text-red-500'}`}>{getItemAvailability(item)}</p> ) : null}</div>{showPrice() ? (   <div className="text-sm font-medium">{formatItemPrice(getItemTotalPrice(item))}</div> ) : null}</div> ))}</div>{items().length === 0 ? (   <p className="text-gray-500 italic text-sm">{getLabel('noItems', 'No items in cart.')}</p> ) : null}</div>   ); }       export default ItemsOverview;


