import type { ProductGridComposableConfig, ProductGridComposable } from './types';
/**
 * Creates a product grid composable -- framework-agnostic state + actions for
 * product listing, search, filtering, sorting, and pagination.
 */
export declare function createProductGridComposable(config: ProductGridComposableConfig): ProductGridComposable;
//# sourceMappingURL=ProductGridComposable.d.ts.map