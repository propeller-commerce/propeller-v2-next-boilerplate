{
  "name": "prepr-cms-seed",
  "version": "1.0.0",
  "private": true,
  "description": "Seed script for provisioning Prepr CMS content models",
  "scripts": {
    "seed": "ts-node scripts/seed-models.ts"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0",
    "dotenv": "^16.0.0"
  }
}
